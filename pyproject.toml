[project]
name = "meshcompute"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "colorama>=0.4.6",
    "psutil>=7.2.2",
    "websockets>=13.1",
]
